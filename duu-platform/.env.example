# Optional. If set, the AI content idea generator and campaign generator will
# call the real Anthropic API. If left unset, the app automatically falls
# back to a built-in, rule-based generator that still uses the dataset's
# performance signals — the app is fully functional either way.
ANTHROPIC_API_KEY=
